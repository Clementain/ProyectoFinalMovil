import React from 'react';
import { IonContent, IonPage, IonHeader, IonTitle } from '@ionic/react';

const TrackingCalendarPage = () => {
    return (
        <IonPage>
            <IonHeader>
                <IonTitle>Tracking Calendar</IonTitle>
            </IonHeader>
            <IonContent>
                {/* Aquí puedes agregar el calendario interactivo */}
            </IonContent>
        </IonPage>
    );
};

export default TrackingCalendarPage;
