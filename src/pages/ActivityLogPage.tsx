import React from 'react';
import { IonContent, IonPage, IonHeader, IonTitle } from '@ionic/react';

const ActivityLogPage = () => {
    return (
        <IonPage>
            <IonHeader>
                <IonTitle>Activity Log</IonTitle>
            </IonHeader>
            <IonContent>
                {/* Aquí puedes agregar el formulario de registro de actividad */}
            </IonContent>
        </IonPage>
    );
};

export default ActivityLogPage;
